apiVersion: apps/v1
kind: Deployment
metadata:
  name: "fluentdserver"
  labels:
    provider: aosqe
    component: "fluentdserver"
    logging-infra: "fluentdserver"
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      provider: aosqe
      component: "fluentdserver"
      logging-infra: "fluentdserver"
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        logging-infra: "fluentdserver"
        provider: aosqe
        component: "fluentdserver"
    spec:
      containers:
      - name: "fluentdserver"
        image: "quay.io/openshiftteststaging/fluentd@sha256:58e76b0114beff0f2fa558daf1ec67142c997898bb494e14716d1ffff381e49b"
        imagePullPolicy: "IfNotPresent"
        args:
        - "fluentd"
        - "-c"
        - "/fluentd/etc/fluent.conf"
        ports:
        - containerPort: 24224
          name: fluentdserver
        securityContext:
          privileged: true
          procMount: Default
        volumeMounts:
        - mountPath: /fluentd/etc
          name: config
          readOnly: true
      serviceAccount: fluentdserver
      serviceAccountName: fluentdserver
      volumes:
      - configMap:
          defaultMode: 420
          name: fluentdserver
        name: config
