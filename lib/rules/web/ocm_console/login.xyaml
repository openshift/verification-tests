login_uhc_console:
  url: /beta/openshift
  action: login_uhc_sequence
login_uhc_sequence:
  action: login_uhc_page_loaded
  action: login_uhc_portal

login_uhc_page_loaded:
  element:
    selector:
      css: "#kc-form-login, #password, #username"
    timeout: 30
login_uhc_portal:
  elements:
    - selector:
        id: username
      op: send_keys <username>
      type: input
    - selector:
        id: password
      op: send_keys <password>
      type: input
  elements:
    - selector:
        id: _eventId_submit
      type: button
      op: click